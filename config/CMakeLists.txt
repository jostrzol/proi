file(GLOB CONFIG_FILES ${CMAKE_CURRENT_SOURCE_DIR}/*)
list(REMOVE_ITEM CONFIG_FILES ${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists.txt)

foreach(FILE IN LISTS CONFIG_FILES)
    configure_file(${FILE} ${CMAKE_BINARY_DIR} COPYONLY)
endforeach()